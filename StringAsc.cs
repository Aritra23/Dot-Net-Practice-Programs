using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace MettlHandson
{
    class StringAsc
    {
        //static void Main(string[] args)
        //{
        //    string[] st = { "abcfsdf", "rt", "mjgsf", "bn" };

        //    SortedDictionary<string, int> sd = new SortedDictionary<string, int>();

        //    for (int i = 0; i < st.Length; i++)
        //    {
        //        sd.Add(st[i], st[i].Length);
        //    }
        //    var sortedDict = from entry in sd orderby entry.Value ascending select entry;
        //    foreach (var item in sortedDict)
        //    {
        //        Console.WriteLine("key :{0} and value : {1}", item.Key, item.Value);
        //    }

        //    Console.ReadLine();
        //}
    }
}
